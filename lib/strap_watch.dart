import 'package:flutter/material.dart';

class StrapWatch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Strap Watch (Analog)',
        style: TextStyle(fontSize: 24),
      ),
      alignment: Alignment.center,
    );
  }
}
